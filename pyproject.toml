[project]
name = "signal-router"
version = "0.1.0"
description = "WhatsApp notification router for HackerRank Orchestrate (Aug 2026)."
requires-python = ">=3.11,<3.12"
dependencies = [
    "anthropic>=0.40",
    "pandas>=2.2",
    "python-dotenv>=1.0",
    "scikit-learn>=1.5",
]

[tool.uv]
package = false
