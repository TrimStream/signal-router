"""signal-router: WhatsApp notification routing for HackerRank Orchestrate.

Modules live under ``code/router/`` rather than ``code/`` directly because
``code`` is a Python standard-library module name. Adding ``code/`` to
``sys.path`` and importing ``router.*`` avoids shadowing it.
"""
